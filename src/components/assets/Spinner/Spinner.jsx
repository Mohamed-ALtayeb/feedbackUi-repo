import spinner from "./spinner.gif";
const Spinner = () => {
  return (
    <img
      src={spinner}
      alt="spinner"
      style={{ width: "50%", margin: "auto", display: "block" }}
    ></img>
  );
};

export default Spinner;
